version: '3.8'

services:
  agentpoker:
    build: .
    container_name: agentpoker
    restart: unless-stopped
    ports:
      - "9003:8080"
    shm_size: 256m
    env_file:
      - .env
    volumes:
      - ./models:/app/models
      - ./data:/app/data
      - ./logs:/app/logs
